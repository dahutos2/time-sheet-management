{% extends "blog/base.html" %}

{% block main %}
<h2>新規投稿</h2>
<a href="{% url 'blog:create' month_previous.year month_previous.month  %}">前月</a>
{{ month_current | date:"Y年m月" }}
<a href="{% url 'blog:create' month_next.year month_next.month %}">次月</a>
<form action="" method="POST">
<table class="table">
    <thead>
    <tr>
    {% for w in week_names %}
        {% if w == '土' %}
        <td class="table-primary">
        {% elif w == '日' %}
        <td class="table-success">
        {% else %}
        <th>
        {% endif %}      
        {{ w }}</th>
        {% endfor %}
    </tr>
        </thead>
        <tbody>
        {% for week_day_forms in month_day_forms %}
        <tr>
            {% for day, forms in week_day_forms.items %}
                <td>
                <div>
                {% if month_current.month != day.month %}
                    {{ day | date:"m/d" }}
                {% else %}
                    {{ day.day }}
                {% endif %}
                    {% for field in form %}
                        {{ field.as_p }}
                    {% endfor %}
                </div>
                </td>
            {% endfor %}
        </tr>
        {% endfor %}
        </tbody>
    </table>
     {% csrf_token %}
    <input type="submit" value="投稿" class="btn btn-primary" />
</form>
{% endblock %}
